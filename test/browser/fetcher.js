'use strict';

var test = require('tape');
var sinon = require('sinon');
var proxyquire = require('proxyquire');

test('fetcher fetches urls', function (t) {
  var fetcher = require('../../browser/fetcher');
  t.end();
});
